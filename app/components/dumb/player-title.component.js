/**
 * @author arman
 * @since 12/29/16
 *
 */

import React from 'react';

const Title = ({text}) => {
    return (
        <h1 className="title deep-shadow">{text}</h1>
    );
};

export default Title;